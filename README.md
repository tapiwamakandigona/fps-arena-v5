# FPS Arena — CI branch

This branch only carries the packaging workflow (Android APK + Windows EXE).
It builds the private source repo via the `SRC_REPO_TOKEN` secret.
The playable web build lives on `gh-pages`: https://tapiwamakandigona.github.io/fps-arena-v5/
